import { InjectionToken } from '@angular/core';
import { TagValue } from './tag-value';

export const BlockShellTag = new InjectionToken('tag.block-shell');
export const BlockShellTagValue: TagValue = 'block-shell';
