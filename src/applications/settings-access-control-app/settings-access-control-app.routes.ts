import { Route } from '@angular/router';
import { SettingsAccessControlComponent } from './settings-access-control.component';

export const routing: Route[] = [
  { path: '', component: SettingsAccessControlComponent }
];
